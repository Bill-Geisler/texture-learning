# MATLAB
*.asv
*.m~
*.mex*
slprj/

# Derived/generated data (regenerated by the pipeline) — keep data/models (shipped artifacts)
data/derived/
results*.mat

# Large shared data is NOT stored here; see config.m paths.data_root (global_data)

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
