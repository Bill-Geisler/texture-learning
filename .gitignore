# MATLAB
*.asv
*.m~
*.mex*
slprj/
cnn/training_monitor_*.png

# Derived/generated data (regenerated by the pipeline) — keep data/models (shipped artifacts)
data/stimuli/
results*.mat

# Large shared data is NOT stored here; see config.m paths.data_root (vislab_data)

# OS / editor
.DS_Store
Thumbs.db
desktop.ini

# Old unmaintained code
Bill's old code/
